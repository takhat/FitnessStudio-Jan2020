@model FitnessStudioApp.CustomerAccount

@{
    ViewData["Title"] = "GetClassPass";
}

<h1>GetClassPass</h1>

<h4>CustomerAccount</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="GetClassPass">

            <input type="hidden" asp-for="CustomerID" />

            <div class="form-group">
                <label asp-for="ClassTitle" class="control-label"></label>
                <select asp-for="ClassTitle" asp-items="Html.GetEnumSelectList<TitleOfClass>()"class="form-control"></select>
                
            </div>
            <div class="form-group">
                <label asp-for="TypeOfClassPass" class="control-label"></label>
                <select asp-for="TypeOfClassPass" asp-items="Html.GetEnumSelectList<ClassPassOption>()" class="form-control"></select>
              
            </div>
         <div class="form-group">
                <input type="submit" value="Buy" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
